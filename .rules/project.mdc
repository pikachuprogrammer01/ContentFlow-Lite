---
description: ContentFlow Lite 项目级决策与默认配置
alwaysApply: true
---

# Project Rules

## 一、核心原则

MVP 优先；保持模块边界清晰；新增能力先更新 PRD/SPEC。

---

## 二、默认模型配置

AI 模型决策受 `.rules/provider.mdc` 约束，这里列出简洁摘要：

| 用途 | 可选模型 | 接入方式 |
|------|---------|----------|
| 文字生成 | Gemini 2.0 Flash | Google AI Studio |
| 文字生成 | DeepSeek V4 Flash | deepseek.com API |
| 文字生成 | 硅基流动 Qwen2.5-72B | SiliconFlow API |
| 图片生成 | 通义万相 2.0 | 阿里云 DashScope |

**不做自动切换。** 用户手动选择 Provider，给了哪个 Key 用哪个。

所有 Provider 实现必须遵守 `.rules/provider.mdc` 的架构约束。

---

## 三、后端与数据持久化

后端架构受 `.rules/backend.mdc` 约束，摘要：

| 层 | 技术 |
|---|---|
| API 框架 | Express |
| 数据库 | MySQL 兼容数据库（本地 MySQL / TiDB / MariaDB 等） |
| 驱动 | mysql2 + TypeORM |
| 数据库管理 | AdminJS |
| 认证 | JWT + bcryptjs |
| 缓存 | Redis（可选，API Key 热缓存） |

- 八张核心表：`users` / `contents` / `prompt_templates` / `prompt_versions` / `generation_records` / `user_settings` / `publish_records` / `app_logs`
- 无离线模式。前端通过 `HttpRepository` 走后端 API，数据库不可用时 App 不可用。

---

## 四、前端技术选型

| 层 | 技术 | 版本约束 |
|---|---|---|
| 框架 | Vue 3 | Composition API + `<script setup>` |
| 语言 | TypeScript | 6.x，严格模式 |
| 构建 | Vite | 8.x |
| 状态 | Pinia | 3.x |
| 路由 | Vue Router | 4.x |
| 组件库 | Naive UI | Vue 3 原生、TypeScript-first、tree-shaking |

---

## 五、包管理

- 必须使用 pnpm
- `pnpm-lock.yaml` 必须提交
- `package.json` 中 `packageManager` 字段指定 pnpm 版本

---

## 六、外部依赖原则

- 优先使用项目已有模块
- 新增 npm 包需评估必要性
- 禁止引入与已有模块职责重叠的库
