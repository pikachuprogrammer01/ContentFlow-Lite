---
description: ContentFlow Lite AI Provider 配置规范与默认模型决策
alwaysApply: true
---

# Provider Rules — AI 模型配置与决策

## 一、默认模型决策（2026-07-03 确定）

本文件记录 ContentFlow Lite 的默认 AI 模型选型和 Provider 架构约束。
所有代码生成、Provider 实现、模型切换都必须遵守本文档。

---

## 二、文字生成（Text Generation）

### 2.1 默认方案：双模型策略

| 角色 | 模型 | 接入方式 | 说明 |
|------|------|----------|------|
| **主模型** | Gemini 2.0 Flash | Google AI Studio API | 免费层，性价比极高，中文良好 |
| **兜底模型** | DeepSeek V4 Flash | deepseek.com API | 中文原生，极低成本，国内直连 |

### 2.2 切换逻辑

```
生成请求 → Gemini 2.0 Flash
              │
              ├── 成功 → 返回结果
              │
              └── 失败/超时/限流 → DeepSeek V4 Flash
                                        │
                                        ├── 成功 → 返回结果
                                        │
                                        └── 失败 → 返回错误给用户
```

实现要求：
- 每次生成只切换一次（不允许无限重试切换）
- 切换时记录日志（便于排查主模型问题）
- 用户在 UI 中可感知当前使用的模型

### 2.3 模型能力要求

文字生成 Provider 必须满足：
- ✅ 支持中文内容生成（小红书、抖音等社交平台文案）
- ✅ 支持 JSON 结构化输出（`response_format` 或 `response_mime_type`）
- ✅ 单次输出 ≥ 2000 tokens（覆盖完整图文内容）
- ✅ API 兼容 OpenAI 格式（兼容现有 `FinalPrompt` 数据结构）

### 2.4 成本参考

| 模型 | Input ($/1M tokens) | Output ($/1M tokens) | 单篇文章成本 |
|------|---------------------|----------------------|-------------|
| Gemini 2.0 Flash | ~$0.10 | ~$0.40 | ≈0.7 分钱 |
| DeepSeek V4 Flash | $0.14 | $0.28 | ≈0.7 分钱 |

---

## 三、图片生成（Image Generation）

### 3.1 默认方案：双模型策略

| 角色 | 模型 | 接入方式 | 说明 |
|------|------|----------|------|
| **主模型** | FLUX.1 [schnell] | 硅基流动 SiliconFlow API | 无需绑卡，1000 张/月免费，Apache 2.0 开源 |
| **兜底模型** | 通义万相 2.0 | 阿里云 DashScope API | 国产画质天花板，中文 prompt 原生理解 |

### 3.2 切换逻辑

```
图片生成请求 → 硅基流动 (FLUX.1 schnell)
                    │
                    ├── 成功 → 返回图片
                    │
                    └── 失败/超时/限流 → 阿里云通义万相
                                              │
                                              ├── 成功 → 返回图片
                                              │
                                              └── 失败 → 返回错误给用户
```

实现要求：
- 每次生成只切换一次
- 切换时记录日志
- 通义万相中文 prompt 不需要翻译，FLUX 需要将中文 prompt 翻译为英文

### 3.2 模型能力要求

图片生成 Provider 必须满足：
- ✅ 支持文生图（Text-to-Image）
- ✅ 支持从 `Page.imagePrompt` 字段读取生成提示
- ✅ 返回图片 URL 或 Base64
- ❌ 图片生成是异步扩展能力，不影响文字生成主流程

### 3.3 触发规则（重要）

- **必须由用户手动点击触发**，绝不自动生成图片
- 编辑页每页有「生成配图」按钮，用户点击后才调图片 Provider
- 生成前用户可修改 imagePrompt
- 用户不填 imagePrompt → 使用系统默认 prompt（page.title + 100 字正文 + clean style）
- 默认 prompt 设计不过度，参考 `.rules/prompt.mdc` 第十四条

### 3.4 成本参考

| 平台 | 模型 | 免费额度 | 超出后单价 | 是否需要绑卡 |
|------|------|----------|-----------|-------------|
| 硅基流动 | FLUX.1 schnell | 1000 张/月 | ¥0.02-0.10/张 | ❌ 不需要 |
| 阿里云通义万相 | 通义万相 2.0 | 100-200 张（新用户） | ¥0.04-0.10/张 | ❌ 不需要（需实名） |

### 3.4 选型依据

- ❌ Together AI — 免费额度需要绑信用卡，淘汰
- ❌ Replicate — 同样需要绑卡，且需翻墙，淘汰
- ✅ 硅基流动 — 国内原生、无需绑卡、FLUX 同款模型、免费额度最慷慨
- ✅ 通义万相 — 中文 prompt 理解远超 FLUX，生成小红书封面时不需要翻译成英文

---

## 四、Provider 架构约束

### 4.1 接口实现规范

所有 Provider 必须实现 `AIProvider` 接口：

```ts
interface AIProvider {
  readonly name: string;    // Provider 唯一标识
  readonly model: string;   // 模型名称
  generate(prompt: FinalPrompt): Promise<string>;
}

interface ImageProvider {
  readonly name: string;
  readonly model: string;
  generate(prompt: string): Promise<ImageResult>;
}
```

### 4.2 Provider 开发规则

1. **一个文件一个 Provider** — `src/providers/` 下每个 Provider 一个文件
2. **通过 `registerProvider()` 注册** — 不直接修改 Workflow
3. **Provider 不含业务逻辑** — 只负责调用 AI，不解析/校验/存储
4. **错误统一封装为 `WorkflowError`** — code 使用 `PROVIDER_ERROR`
5. **支持超时控制** — 默认 30s 超时，可配置

### 4.3 禁止事项

- ❌ 禁止在 Provider 中解析 AI 返回内容（那是 Parser 的职责）
- ❌ 禁止在 Provider 中保存数据（那是 Repository 的职责）
- ❌ 禁止在 Provider 中拼接 Prompt（那是 PromptBuilder 的职责）
- ❌ 禁止页面直接调用 Provider（必须通过 Workflow）
- ❌ 禁止硬编码 API Key（必须从环境变量或用户配置读取）

---

## 五、API Key 管理

### 5.1 开发环境

```
VITE_GEMINI_API_KEY=        # Google AI Studio
VITE_DEEPSEEK_API_KEY=      # DeepSeek Platform
VITE_SILICONFLOW_API_KEY=   # 硅基流动 SiliconFlow
VITE_TONGYI_API_KEY=        # 阿里云 DashScope（通义万相）
```

### 5.2 生产环境

- 提供设置页面，允许用户填写自己的 API Key
- Key 存储在 `localStorage`（通过 `Repository`）
- 不将 Key 上传到任何服务器

### 5.3 获取 API Key

| 平台 | 注册地址 | 新用户福利 | 是否需要绑卡 |
|------|---------|-----------|-------------|
| Google AI Studio | aistudio.google.com | 免费层，无需付费 | ❌ |
| DeepSeek | platform.deepseek.com | 新用户送 ¥10 额度 | ❌ |
| 硅基流动 | siliconflow.cn | 1000 张/月免费 | ❌ |
| 阿里云 DashScope | dashscope.aliyun.com | 100-200 张免费（新用户） | ❌（需实名） |

---

## 六、Provider 目录结构

```
src/providers/
├── index.ts                    # registerProvider / callProvider 入口
├── gemini-provider.ts          # Gemini 2.0 Flash（文字-主）
├── deepseek-provider.ts        # DeepSeek V4 Flash（文字-兜底）
├── mock-provider.ts            # Mock（开发用，默认不启用）
├── flux-provider.ts            # 硅基流动 FLUX.1 schnell（图片-主）
├── tongyi-provider.ts          # 阿里云通义万相（图片-兜底）
└── types.ts                    # Provider 类型扩展（ImageProvider 等）
```

---

## 七、扩展规则

### 7.1 新增文字模型

1. 在 `src/providers/` 创建新文件 `xxx-provider.ts`
2. 实现 `AIProvider` 接口
3. 调用 `registerProvider()` 注册
4. 更新本文件的模型决策表
5. **不修改** Workflow / Parser / Store / 页面

### 7.2 新增图片模型

1. 在 `src/providers/` 创建新文件 `xxx-image-provider.ts`
2. 实现 `ImageProvider` 接口
3. 注册到图片 Provider 注册表
4. 更新本文件
5. **不修改** 文字生成流程

---

## 八、决策记录

| 日期 | 决策 | 原因 |
|------|------|------|
| 2026-07-03 | 文字主模型 = Gemini 2.0 Flash | 免费层、中文质量好、JSON 模式原生支持 |
| 2026-07-03 | 文字兜底 = DeepSeek V4 Flash | 中文原生、成本最低、国内直连 |
| 2026-07-03 | 图片主模型 = FLUX.1 schnell via 硅基流动 | 国内原生、无需绑卡、免费额度最慷慨（1000 张/月） |
| 2026-07-03 | 图片兜底 = 通义万相 via 阿里云 DashScope | 国产画质天花板，中文 prompt 原生理解 |
| 2026-07-03 | 不选 Together AI / Replicate | 需要绑信用卡，或需要翻墙 |
| 2026-07-03 | 不选 Claude / GPT-4o | 成本高、中文非原生、需要海外访问 |
