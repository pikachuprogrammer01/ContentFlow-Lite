---
description: ContentFlow Lite AI Provider 配置规范与默认模型决策
alwaysApply: true
---

# Provider Rules — AI 模型配置与决策

> 版本：2026-07-05 · 对标 `docs/SPEC.md`


## 一、默认模型决策（2026-07-03 确定）

本文件记录 ContentFlow Lite 的默认 AI 模型选型和 Provider 架构约束。
所有代码生成、Provider 实现、模型选择都必须遵守本文档。

---

## 二、文字生成（Text Generation）

### 2.1 默认方案

| 角色 | 模型 | 接入方式 | 说明 |
|------|------|----------|------|
| **可选模型** | Gemini 2.0 Flash | Google AI Studio API | 免费层，性价比极高，中文良好 |
| **可选模型** | DeepSeek V4 Flash | deepseek.com API | 中文原生，极低成本，国内直连 |
| **可选模型** | 硅基流动 Qwen2.5-72B | SiliconFlow API | 国内直连、免费额度、OpenAI 兼容 |

### 2.2 切换策略：无自动切换

**不做自动 fallback。** 用户手动选择使用哪个 Provider，给了哪个 Provider 的 API Key 就用哪个。

后台不替用户做模型切换决策。用户选了 Gemini，Gemini 失败 → 报错返回，不切 DeepSeek。

### 2.3 模型能力要求

文字生成 Provider 必须满足：
- ✅ 支持中文内容生成（小红书、抖音等社交平台文案）
- ✅ 支持 JSON 结构化输出（`response_format` 或 `response_mime_type`）
- ✅ 单次输出 ≥ 2000 tokens（覆盖完整图文内容）
- ✅ API 兼容 OpenAI 格式（兼容现有 `FinalPrompt` 数据结构）

### 2.4 成本参考

| 模型 | Input ($/1M tokens) | Output ($/1M tokens) | 单篇文章成本 |
|------|---------------------|----------------------|-------------|
| Gemini 2.0 Flash | ~$0.10 | ~$0.40 | ≈0.7 分钱 |
| DeepSeek V4 Flash | $0.14 | $0.28 | ≈0.7 分钱 |

---

## 三、图片生成（Image Generation）

### 3.1 默认方案：单一模型

| 角色 | 模型 | 接入方式 | 说明 |
|------|------|----------|------|
| **默认模型** | 通义万相 2.0 | 阿里云 DashScope API | 国产画质天花板，中文 prompt 原生，无需翻译 |

不引入 FLUX / Stable Diffusion 等国外模型。所有图片 prompt 均为中文。

### 3.2 模型能力要求

图片生成 Provider 必须满足：
- ✅ 支持文生图（Text-to-Image）
- ✅ 中文 prompt 原生支持
- ✅ 支持从 `Page.imagePrompt` 字段读取生成提示
- ✅ 返回图片 URL 或 Base64
- ❌ 图片生成是异步扩展能力，不影响文字生成主流程

### 3.3 触发规则（重要）

- **必须由用户手动点击触发**，绝不自动生成图片
- 编辑页每页有「生成配图」按钮，用户点击后才调图片 Provider
- 生成前用户可修改 imagePrompt
- 用户不填 imagePrompt → 使用系统默认 prompt（中文）
- 默认 prompt 设计参考 `.rules/prompt.mdc` 第十三条

### 3.4 成本参考

| 平台 | 模型 | 免费额度 | 超出后单价 | 是否需要绑卡 |
|------|------|----------|-----------|-------------|
| 阿里云通义万相 | 通义万相 2.0 | 100-200 张（新用户） | ¥0.04-0.10/张 | ❌ 不需要（需实名） |

### 3.5 选型依据

- ✅ 通义万相 — 中文 prompt 原生理解，不引入英文翻译环节
- ❌ FLUX.1 schnell — 需要中→英翻译，增加复杂度与不确定性
- ❌ Together AI / Replicate — 需要绑信用卡或翻墙

---

## 四、Provider 架构约束

### 4.1 接口实现规范

所有 Provider 必须实现 `AIProvider` 接口：

```ts
interface AIProvider {
  readonly name: string;    // Provider 唯一标识
  readonly model: string;   // 模型名称
  generate(prompt: FinalPrompt): Promise<string>;
}

interface ImageProvider {
  readonly name: string;
  readonly model: string;
  generate(prompt: string): Promise<ImageResult>;
}
```

### 4.2 Provider 开发规则

1. **一个文件一个 Provider** — `server/providers/` 下每个 Provider 一个文件
2. **通过 `registerProvider()` 注册** — 不直接修改 Workflow
3. **Provider 不含业务逻辑** — 只负责调用 AI，不解析/校验/存储
4. **错误统一封装为 `WorkflowError`** — code 使用 `PROVIDER_ERROR`
5. **支持超时控制** — 文字模型默认 30s，图片模型通过 `IMAGE_TIMEOUT` 环境变量配置（默认 15s）
6. **图片超时处理** — 超时后 UI 显示「重试」按钮，用户手动点击重试（仅 1 次）。不自动重试
7. **Provider 运行在后端** — `server/providers/`，通过 Express API 调用，AI Key 从后端 DB 读取

### 4.3 禁止事项

- ❌ 禁止在 Provider 中解析 AI 返回内容（那是 Parser 的职责）
- ❌ 禁止在 Provider 中保存数据（那是 Repository 的职责）
- ❌ 禁止在 Provider 中拼接 Prompt（那是 PromptBuilder 的职责）
- ❌ 禁止页面直接调用 Provider（必须通过 Workflow）
- ❌ 禁止硬编码 API Key（必须从后端 `user_settings` 表读取）
- ❌ 禁止做自动 Provider 切换

---

## 五、API Key 管理

### 5.1 存储方式

API Key 存储在**后端 MySQL `user_settings` 表**中，AES-256-GCM 加密落库。Redis 可选热缓存明文（TTL 1h）。

前端不存 API Key。API 不返回实际 Key 值（仅返回 `isSet: boolean`）。敏感数据仅保存时展示一次。

详细规范参见 `.rules/backend.mdc` §7.4。

### 5.2 获取 API Key

| 平台 | 注册地址 | 新用户福利 | 是否需要绑卡 |
|------|---------|-----------|-------------|
| Google AI Studio | aistudio.google.com | 免费层，无需付费 | ❌ |
| DeepSeek | platform.deepseek.com | 新用户送 ¥10 额度 | ❌ |
| 硅基流动 | cloud.siliconflow.cn | 注册送 2000 万 tokens | ❌ |
| 阿里云 DashScope | dashscope.aliyun.com | 100-200 张免费（新用户） | ❌（需实名） |

---

## 六、Provider 目录结构

```
server/providers/
├── index.ts                    # registerProvider / callProvider 入口
├── gemini-provider.ts          # Gemini 2.0 Flash（文字）
├── deepseek-provider.ts        # DeepSeek V4 Flash（文字）
├── siliconflow-provider.ts     # 硅基流动 Qwen2.5-72B（文字）
├── mock-provider.ts            # Mock（开发用，默认不启用）
├── tongyi-provider.ts          # 阿里云通义万相（图片）
└── types.ts                    # Provider 类型扩展（ImageProvider 等）
```

---

## 七、SDK 依赖与初始化（2026-07-03 更新）

### 7.1 依赖清单

```json
{
  "openai": "^6.45",
  "@google/genai": "^2.10"
}
```

注意：`@google/generative-ai` 已废弃，必须用 `@google/genai`。

### 7.2 Gemini Provider（文字）

```ts
// server/providers/gemini-provider.ts
import { GoogleGenAI } from '@google/genai';

const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY });

class GeminiProvider implements AIProvider {
  readonly name = 'Gemini';
  readonly model = 'gemini-2.0-flash';

  async generate(prompt: FinalPrompt): Promise<string> {
    const response = await ai.models.generateContent({
      model: this.model,
      contents: `${prompt.systemPrompt}\n\n${prompt.userPrompt}`,
      config: { responseMimeType: 'application/json' },
    });
    return response.text ?? '';
  }
}
```

### 7.3 DeepSeek Provider（文字）

```ts
// server/providers/deepseek-provider.ts
import OpenAI from 'openai';

const client = new OpenAI({
  baseURL: 'https://api.deepseek.com',
  apiKey: process.env.DEEPSEEK_API_KEY,
});

class DeepSeekProvider implements AIProvider {
  readonly name = 'DeepSeek';
  readonly model = 'deepseek-v4-flash';

  async generate(prompt: FinalPrompt): Promise<string> {
    const completion = await client.chat.completions.create({
      model: this.model,
      messages: [
        { role: 'system', content: prompt.systemPrompt },
        { role: 'user', content: prompt.userPrompt },
      ],
      response_format: { type: 'json_object' },
    });
    return completion.choices[0].message.content ?? '';
  }
}
```

### 7.4 SiliconFlow Provider（文字-备选）

```ts
// server/providers/siliconflow-provider.ts
import OpenAI from 'openai';

const client = new OpenAI({
  baseURL: 'https://api.siliconflow.cn/v1',
  apiKey: process.env.SILICONFLOW_API_KEY,
});

class SiliconFlowProvider implements AIProvider {
  readonly name = 'SiliconFlow';
  readonly model = 'Qwen/Qwen2.5-72B-Instruct';

  async generate(prompt: FinalPrompt): Promise<string> {
    const completion = await client.chat.completions.create({
      model: this.model,
      messages: [
        { role: 'system', content: prompt.systemPrompt },
        { role: 'user', content: prompt.userPrompt },
      ],
      response_format: { type: 'json_object' },
    });
    return completion.choices[0].message.content ?? '';
  }
}
```

### 7.5 通义万相 Provider（图片）

```ts
// server/providers/tongyi-provider.ts
import OpenAI from 'openai';

const client = new OpenAI({
  baseURL: 'https://dashscope.aliyuncs.com/compatible-mode/v1',
  apiKey: process.env.TONGYI_API_KEY,
});

class TongyiProvider implements ImageProvider {
  readonly name = 'Tongyi-Wanxiang';
  readonly model = 'wanxiang-v2';

  async generate(prompt: string): Promise<ImageResult> {
    const result = await client.images.generate({
      model: this.model,
      prompt,
      n: 1,
    });
    return { url: result.data[0].url ?? '' };
  }
}
```

### 7.6 baseURL 汇总

| Provider | 包 | baseURL |
|---|---|---|
| Gemini | `@google/genai` | —（SDK 内建） |
| DeepSeek | `openai` | `https://api.deepseek.com` |
| SiliconFlow | `openai` | `https://api.siliconflow.cn/v1` |
| 通义万相 | `openai` | `https://dashscope.aliyuncs.com/compatible-mode/v1` |

---

## 八、扩展规则

### 8.1 新增文字模型

1. 在 `server/providers/` 创建新文件 `xxx-provider.ts`
2. 实现 `AIProvider` 接口
3. 调用 `registerProvider()` 注册
4. 更新本文件的模型决策表
5. **不修改** Workflow / Parser / Store / 页面

### 8.2 新增图片模型

1. 在 `server/providers/` 创建新文件 `xxx-image-provider.ts`
2. 实现 `ImageProvider` 接口
3. 注册到图片 Provider 注册表
4. 更新本文件
5. **不修改** 文字生成流程

---

## 九、决策记录

| 日期 | 决策 | 原因 |
|------|------|------|
| 2026-07-03 | 文字主模型 = Gemini 2.0 Flash | 免费层、中文质量好、JSON 模式原生支持 |
| 2026-07-03 | 文字备选 = DeepSeek V4 Flash | 中文原生、成本最低、国内直连 |
| 2026-07-03 | 图片 = 通义万相（单一模型） | 中文原生，不引入 FLUX，不做中英翻译 |
| 2026-07-03 | 不选 FLUX / Together AI / Replicate | 需要翻译英文或绑信用卡，淘汰 |
| 2026-07-03 | 不选 Claude / GPT-4o | 成本高、中文非原生、需要海外访问 |
| 2026-07-03 | 不做自动 Provider 切换 | 用户手动选择 Provider，给了哪个 Key 就用哪个 |
| 2026-07-03 | Provider 移到后端 `server/providers/` | API Key 存后端 DB，AI 调用只能后端发起 |
| 2026-07-03 | API Key 全存后端 `user_settings` 表 | AES-256-GCM 加密，前端不回显，不做 localStorage |
| 2026-07-03 | 文字备选 = 硅基流动 Qwen2.5-72B | 国内直连、免费额度、OpenAI 兼容、中文原生 |
