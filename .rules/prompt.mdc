---
description: ContentFlow Lite Prompt 体系与生成规范
alwaysApply: true
---

# Prompt Rules

## 一、核心定位

Prompt 是 ContentFlow Lite 的内容生成核心资产。

所有 AI 输出质量，完全由 Prompt 系统决定。

系统中所有 Prompt 必须具备：

- 可版本化
- 可追溯
- 可复现
- 可结构化

任何未进入 Prompt 体系的内容生成请求，都属于非法流程。

---

## 二、Prompt 生命周期

Prompt 的完整生命周期如下：

```text
Template
   ↓
Variables 注入
   ↓
Prompt Builder
   ↓
Prompt Version 绑定
   ↓
Final Prompt
   ↓
AI Execution
```

---

## 三、Prompt Builder 规范

Prompt Builder 是唯一允许生成 Prompt 的模块。

### Builder 职责：

- 读取 Template
- 注入变量
- 注入 Metadata
- 绑定 Prompt Version
- 生成 Final Prompt

---

### Builder 禁止行为：

- 不得调用 AI
- 不得保存数据
- 不得解析 Response
- 不得修改 DTO
- 不得参与 Workflow 控制

---

## 四、Prompt Template 规范

Template 是 Prompt 的静态定义。

### 必须包含：

- 角色定义（Role）
- 输出格式约束
- 内容结构要求
- 平台规则
- 禁止行为说明

---

### Template 示例结构：

```text
你是一名内容创作者。

请根据以下要求生成内容：

主题：{{topic}}
平台：{{platform}}

要求：
1. 生成标题
2. 生成封面文案
3. 分页输出正文
4. 输出标签
5. 必须返回 JSON
6. 必须符合 Content DTO 结构
```

---

## 五、Variables 规则

变量由 Workflow 提供。

允许变量包括：

- topic
- platform
- audience
- tone
- extra_requirements

---

### 禁止行为：

- 页面拼接变量
- Builder 内部生成业务变量
- AI 自动生成变量
- Prompt 中硬编码业务数据

---

## 六、Prompt Version 规范

Prompt Version 是系统核心机制之一。

### 每次生成必须记录：

- Prompt ID
- Prompt Version
- Model
- Platform
- Timestamp

---

### Version 规则：

- Version 必须不可变
- 修改 Prompt 必须生成新 Version
- 历史 Version 不可覆盖
- 不允许动态修改 Version 内容

---

### Version 作用：

- 内容可复现
- AI 输出可对比
- Prompt 优化可追踪

---

## 七、Final Prompt 规则

Final Prompt 是最终发送给 AI 的内容。

### 必须满足：

- 完整 Template + Variables
- 包含 Prompt Version 信息
- 不可二次修改
- 不可在 Provider 内拼接

---

### 禁止：

- Provider 修改 Prompt
- Workflow 动态拼接 Prompt
- 页面直接生成 Prompt

---

## 八、平台 Prompt 规范

不同平台必须使用不同 Template：

### 小红书：

- 强调标题吸引力
- 分段短内容
- 标签明确

### 抖音：

- 强调节奏感
- 口语化表达
- 适合脚本结构

### 公众号：

- 长内容结构
- 分章节
- 逻辑严密

---

## 九、OutputSchema — 输出结构校验

每个 Prompt Template 必须附带 `outputSchema`，用于程序化校验 AI 输出是否符合预期结构。定义以 `docs/types.md` §3.3 为准。

### 定义：

```ts
// 详见 docs/types.md §3.3
interface OutputSchema {
  fields: OutputSchemaField[];
}

interface OutputSchemaField {
  name: string;
  type: 'string' | 'number' | 'array' | 'object';
  required: boolean;
  children?: OutputSchemaField[];  // 嵌套对象/数组的字段
}
```

### 小红书模板示例：

```ts
{
  fields: [
    { name: 'titles', type: 'array', required: true },
    { name: 'cover', type: 'object', required: true,
      children: [
        { name: 'title', type: 'string', required: true },
        { name: 'subtitle', type: 'string', required: false },
      ]
    },
    { name: 'pages', type: 'array', required: true,
      children: [
        { name: 'text', type: 'string', required: true },
        { name: 'imagePrompt', type: 'string', required: false },
      ]
    },
    { name: 'tags', type: 'array', required: true,
      children: [
        { name: '', type: 'string', required: true },  // string[] 元素
      ]
    },
    { name: 'summary', type: 'string', required: true },
  ]
}
```

### 校验规则：

- required 字段全部存在 → 通过
- 字段类型匹配 → 通过
- 数组长度在 minItems~maxItems 之间 → 通过
- 任一不通过 → Validate Node 触发重试（将失败原因注入 prompt）

禁止：没有 outputSchema 的 Template 投入生产、用文字描述代替结构化 schema。

---

## 十、Prompt 错误处理

Prompt 相关错误必须由 Workflow 处理：

| 类型 | 说明 |
|------|------|
| TEMPLATE_ERROR | 模板错误 |
| VARIABLE_ERROR | 变量缺失 |
| VERSION_ERROR | 版本不存在 |
| BUILD_ERROR | Prompt 构建失败 |

---

## 十一、Prompt 扩展规则

允许新增：

- 新 Template
- 新 Platform Prompt
- 新变量类型

禁止：

- 修改已有 Template 结构
- 覆盖历史 Prompt Version
- 在运行时动态修改 Prompt 结构

---

## 十二、Prompt 与 DTO 绑定关系

Prompt 输出必须严格匹配 Content DTO：

- titles → Title[]
- cover → Cover
- pages → Page[]
- tags → string[]
- summary → string

任何不匹配结构必须在 Parser 阶段修正或丢弃。

---

## 十三、图片 Prompt 规范（新增）

文字生成完成后，Page 的 `imagePrompt` 字段用于配图生成。

### 来源优先级：

1. **用户手动输入** — 用户在编辑页为该 Page 填写 → 优先使用
2. **系统默认** — 用户未填写 → 使用默认模板

### 默认 prompt 设计

默认中文 prompt，用户始终看到中文、用中文修改。

```ts
function buildDefaultImagePrompt(page: Page, topic: string): string {
  const snippet = page.content.slice(0, 100);
  return `一张高质量的社交媒体配图。
主题：${topic}
标题：${page.title}
内容摘要：${snippet}
风格：明亮简洁，适合图文展示`;
}
```

### 图片比例

比例由用户在编辑页自行选择，不写死在默认 prompt 中。

UI 提供选择器，默认比例见常量：

```ts
const IMAGE_RATIOS = [
  { value: '1:1',  label: '1:1 正方形' },
  { value: '3:4',  label: '3:4 竖版（推荐）' },
  { value: '4:3',  label: '4:3 横版' },
  { value: '9:16', label: '9:16 全屏竖版' },
  { value: '16:9', label: '16:9 横版宽屏' },
];
```

最终 prompt 由默认模板 + 用户选择的比例拼接而成，发给 Provider。

### 触发规则：

- **必须用户手动点击**「生成配图」按钮，绝不自动生成
- 生成前用户可修改 prompt
- 调用图片 Provider → 返回 URL → 写入 `page.imageUrl`

### Page 类型扩展：

```ts
interface Page {
  id: string;
  title: string;
  content: string;
  imagePrompt?: string;     // 图片 prompt（用户填或系统默认）
  imageUrl?: string;        // 生成后的图片 URL
  imageStatus?: 'idle' | 'generating' | 'done' | 'error';
}
```

---

## 十四、Prompt 安全规则

---

## 十五、总结

Prompt 系统是 ContentFlow Lite 的核心质量控制器。

所有内容质量取决于：

- Prompt Template 设计
- Prompt Builder 规范
- Prompt Version 管理

任何绕过 Prompt 系统的生成方式都属于非法路径。

Prompt 系统必须保证：

> 结构稳定 + 可复现 + 可演进