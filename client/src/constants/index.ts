/**
 * client/src/constants/index.ts — 客户端常量入口
 *
 * 共享常量定义在 @contentflow/shared。
 * 本文件只做重导出，禁止在此新增与 shared 重复的常量。
 */

export {
  PLATFORM_LABEL,
  PLATFORMS,
  PROVIDER_LABEL,
  MAX_RETRY_COUNT,
  DEFAULT_MODEL,
} from '@contentflow/shared/constants';
